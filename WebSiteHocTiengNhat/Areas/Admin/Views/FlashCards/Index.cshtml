@model List<WebSiteHocTiengNhat.Models.Course>
@{
    ViewData["Title"] = "Select the course";
}
<h2>@ViewData["Title"]</h2>
<div style="padding-top:10px">
    <div class="container">
        <div class="row">
            @foreach (var course in Model)
            {
                <div class="col-md-3 d-flex align-items-stretch">
                    <div class="card mb-4 zoom-card" style="width: 100%;" data-url="@Url.Action("LessonList", "FlashCards", new { courseId = course.CourseId })">
                        <img class="card-img-top" src="@course.Image" alt="Card image cap" style="height: 200px; object-fit: cover;">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@course.CourseName</h5>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</div>

<style>
    .zoom-card {
        transition: transform .2s; /* Animation */
    }

        .zoom-card:hover {
            transform: scale(1.05); /* Zoom in */
            cursor: pointer;
        }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        var cards = document.querySelectorAll('.zoom-card');
        cards.forEach(function (card) {
            card.addEventListener('click', function () {
                window.location.href = card.getAttribute('data-url');
            });
        });
    });
</script>
